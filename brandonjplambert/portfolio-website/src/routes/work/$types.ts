import type { PageLoad } from './$types';
import type { PhilosophyPrinciple, TimelineEvent, Testimonial, WorkPageData } from './+page';

export type PageData = WorkPageData;

export { type PhilosophyPrinciple, type TimelineEvent, type Testimonial, type WorkPageData };