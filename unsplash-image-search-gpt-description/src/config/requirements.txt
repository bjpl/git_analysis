# Secure Configuration Management Requirements
# These dependencies are needed for the enhanced security features

# Core dependencies
aiohttp>=3.9.0        # Async HTTP client for API validation
pywin32>=306; sys_platform == "win32"  # Windows DPAPI encryption support

# Optional dependencies for enhanced functionality
cryptography>=41.0.0  # Fallback encryption for non-Windows systems
keyring>=24.0.0       # System keyring integration (optional)

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0