# Git attributes to prevent submodule processing during Netlify builds
# Force text handling and disable LFS for all files

* text=auto
*.js text
*.ts text  
*.tsx text
*.json text
*.md text
*.yml text
*.yaml text

# Explicitly prevent any submodule or LFS processing
.gitmodules -text -merge -diff
**/.gitmodules -text -merge -diff

# Disable LFS for all file types to prevent Netlify issues
* -filter -merge -text

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary